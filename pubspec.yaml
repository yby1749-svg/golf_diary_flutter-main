name: golf_diary_flutter
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  image_picker: ^1.0.7
  pdf: ^3.10.7
  printing: ^5.12.0

  flutter_localizations:
    sdk: flutter

  provider: ^6.1.2
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  path_provider: ^2.1.1



dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/home_background.png
    - assets/courses_new.json
